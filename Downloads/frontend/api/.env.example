# ── MeetRoute 환경변수 예시 ──
# 실제 값은 커밋하지 말고 .env 로 복사해서 사용하세요.

# ────────────────────────────────────────────────────────────────
# 프론트엔드
# ────────────────────────────────────────────────────────────────

# 백엔드 주소. Spring Boot 기본 포트 8080, context-path 없음.
# 엔드포인트는 이 값 뒤에 /api/v1/... 이 붙습니다.
API_BASE_URL=http://localhost:8080

# 번들러를 쓰면 접두사가 필요합니다. 쓰는 것 하나만 남기세요.
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
# VITE_API_BASE_URL=http://localhost:8080
# REACT_APP_API_BASE_URL=http://localhost:8080

# prototype/index.html 은 빌드가 없어 .env 를 읽지 못합니다.
# 기본값 http://localhost:8080 을 쓰며, 바꾸려면 브라우저 콘솔에서:
#   localStorage.setItem('meetroute.apiBase', 'http://192.168.0.10:8080')

# ────────────────────────────────────────────────────────────────
# 백엔드 (unithon-2026/backend) — 참고용
# ────────────────────────────────────────────────────────────────
# 아래는 백엔드 저장소의 application.yaml 이 실제로 요구하는 변수입니다.
# 이 저장소에서 쓰이지는 않지만, 로컬에서 백엔드를 띄울 때 필요합니다.
#
#   DB_URL=jdbc:mysql://localhost:3306/meetroute
#   DB_USER=root
#   DB_PASSWORD=
#   ANTHROPIC_API_KEY=       # AI 영업 브리핑 (POST /shops/{id}/briefing)
#
# ⚠️ ANTHROPIC_API_KEY 가 없으면 애플리케이션이 뜨지 않습니다
#    (application.yaml 의 spring.ai.anthropic.api-key 가 필수 치환값).
#    브리핑 호출이 실패하면 502 + SHOP5001 이 떨어집니다.
#
# ⚠️ MySQL 입니다. 기존 초안에 적혀 있던 PostgreSQL DATABASE_URL 은 실제와 다릅니다.
# ⚠️ JWT_SECRET / JWT_EXPIRES_IN 도 제거했습니다 — 현재 SecurityConfig 는 전체 permitAll 이라
#    인증이 없고, 토큰 발급 코드도 없습니다.

# ────────────────────────────────────────────────────────────────
# 아직 쓰이지 않는 값 (미구현 기능용)
# ────────────────────────────────────────────────────────────────
# 데이터 수집 스케줄러·지오코딩·AI CRM 은 서버에 구현되어 있지 않습니다.
# 해당 기능을 붙일 때 백엔드 쪽 환경변수로 추가하세요.
#
#   PUBLIC_DATA_API_KEY=    # 서울시 위생업소 인허가 OpenAPI
#   MAP_API_KEY=            # 지도 타일·지오코딩 (좌표 변환)
#   COLLECT_RETRY_MAX=3     # 수집 실패 시 자동 재시도 횟수
#
# AI CRM 후속 영업(R-RABMND)은 엔터티부터 없습니다. 붙일 때 필요한 값:
#   CRM_HISTORY_RETENTION_YEARS=3   # 대화 원문·AI 요약 보존 기간 (기획 확정)

# ────────────────────────────────────────────────────────────────
# CORS
# ────────────────────────────────────────────────────────────────
# api/backend-patch/0001-priority-grade-1-4-and-cors.patch 를 적용하면
# 백엔드가 아래 속성으로 CORS 오리진을 받습니다 (콤마 구분, 여러 개 가능).
#
#   app.cors.allowed-origins=http://localhost:3000,http://localhost:5173
#
# 패치 전에는 CORS 설정이 아예 없어 브라우저가 /api 호출을 전부 막습니다.
# (프로토타입이 항상 폴백 배너로 뜨면 대개 이것이 원인입니다.)
CORS_ORIGIN=http://localhost:3000
